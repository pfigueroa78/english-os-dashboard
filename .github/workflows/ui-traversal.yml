name: UI Traversal

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch or commit to test"
        required: false
        default: "coach-pedagogy-restore"
      base_url:
        description: "Deployed URL to test"
        required: false
        default: "https://english-os-dashboard-mkhgqavzi-pedro-figueroa-s-projects.vercel.app"

jobs:
  ui-traversal:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout selected ref
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || 'coach-pedagogy-restore' }}

      - name: Show selected target
        run: |
          echo "Testing ref: ${{ github.event.inputs.ref || 'coach-pedagogy-restore' }}"
          echo "Testing URL: ${{ github.event.inputs.base_url || 'https://english-os-dashboard-mkhgqavzi-pedro-figueroa-s-projects.vercel.app' }}"
          git log -1 --format='%H %s'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run UI traversal tests
        env:
          E2E_BASE_URL: ${{ github.event.inputs.base_url || 'https://english-os-dashboard-mkhgqavzi-pedro-figueroa-s-projects.vercel.app' }}
          E2E_USE_WEB_SERVER: "0"
        run: npm run test:e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload Playwright test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
          retention-days: 7
          if-no-files-found: ignore
